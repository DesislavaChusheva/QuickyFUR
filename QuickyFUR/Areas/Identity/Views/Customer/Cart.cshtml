@using Microsoft.AspNetCore.Identity
@using QuickyFUR.Core.Models

    @addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
    @addTagHelper *, AuthoringTagHelpers
    @model IEnumerable<ProductsInCartViewModel>
@{
        Layout = "_Layout";
}

<h1>Cart</h1>


@if (Model.FirstOrDefault() == null)
{
    <h2>Yor cart is empty!</h2>
}

else
{
    <h2>Yor cart is NOT empty!</h2>

    @*@foreach (var m in Model)
    {
        <div class="row">
            <img src="@m.ImageLink" class="w3-round" alt="Norway">
            <div class="col-md-4">
                <b>Product name: </b>
                <p>"@m.Name"</p>
            </div>
            <div class="col-md-4">
                <b>Designer name: </b>
                <p>"@m.DesignerName"</p>
            </div>
            <div class="col-md-4">
                <b>Product description: </b>
                <p>"@m.Descritpion"</p>
            </div>
            <a href="@m.ConfiguratorLink" target="_blank">Configurator</a>
            <a asp-area="Identity" asp-controller="Customer" asp-action="OrderProduct">Order</a>
        </div>
    }*@
}

